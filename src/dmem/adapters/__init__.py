"""Distribution-surface adapters (MCP server, framework integrations).

All adapters are thin wrappers over `DMemEngine`. They contain no memory logic
of their own — only translation between the surface's protocol and the engine's
methods / tool specs.
"""
